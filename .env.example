# cfddns runtime configuration — copy values into your shell or a root-owned
# env file (deploy/cfddns.env.example). NEVER commit real secrets.
#
# This file is auto-loaded when present (dotenv syntax); values already set in
# the environment win over the file.

CLOUDFLARE_API_TOKEN=
CFDDNS_DB=./cfddns.db
LOG_LEVEL=info
#CF_DDNS_DRY_RUN=1
